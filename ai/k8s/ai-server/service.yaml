apiVersion: v1
kind: Service
metadata:
  name: ai-server-service
  namespace: apps
  labels:
    app: ai-server
spec:
  type: ClusterIP
  selector:
    app: ai-server
  ports:
  - name: http
    protocol: TCP
    port: 8000
    targetPort: 8000